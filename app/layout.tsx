import type { Metadata } from 'next'
import { Inter } from 'next/font/google'
import './globals.css'

const inter = Inter({ subsets: ['latin'] })

export const metadata: Metadata = {
  title: 'ChessMaster Arena - Elite Chess Platform with HD Video Chat',
  description: 'The world\'s most advanced chess platform with HD video chat, AI mastery coaching, and lightning-fast gameplay. Join elite players where legends are born.',
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className={inter.className}>{children}</body>
    </html>
  )
}